# Node 🪦

## Express basic

```js
import express from 'express'

const app = express()

app.get('/', (req, res) => {
    res.send('Hello World')
})

app.listen(3000)
```

### Node Package Manager

> [npm](https://www.npmjs.com/package/damn) is a package manager

### Node Module System

```js
const imports = require('./first-module')

console.log(imports.add(1, 3))
```

### Awesome damn project :0

> This is a description of the project
> And here is a link to [npm](https://www.npmjs.com/package/damn)
> its zero to hero guide is [here](https://www.npmjs.com/package/damn#readme)
> learn
